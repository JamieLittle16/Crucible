# Vanilla Atlas reports

Compact aggregate reports generated from the private local official source corpus may be committed here. They must not contain source bodies.

The SQLite Atlas database itself is generated under `.crucible/vanilla/` and is ignored. Reports are expected to be deterministic for the same source pin and Atlas version.
