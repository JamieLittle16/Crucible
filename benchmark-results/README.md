# Benchmark Results

Committed benchmark evidence belongs here only when it is attached to a reproducible methodology and exact composition identity. Local ad-hoc output belongs in `benchmark-results/local/` and is ignored.

Every qualified result records commit, composition lock/profile, Minecraft pin, toolchain, hardware, OS, workload/world/seed, warmup, raw samples and correctness scope.
